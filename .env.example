# Configurações de Desenvolvimento Local - Template
# Copie este arquivo para .env e ajuste os valores conforme necessário

# JWT Configuration
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Bcrypt Configuration
BCRYPT_ROUNDS=12

# File Upload Configuration (em bytes)
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=*

# Environment
NODE_ENV=development

# Database Configuration (para desenvolvimento local)
# Estas configurações são gerenciadas pelo Wrangler em produção
# DATABASE_URL=file:./dev.db

# Cloudflare R2 Configuration (para desenvolvimento local)
# Em produção, estas são gerenciadas pelo Wrangler
# R2_ACCOUNT_ID=your_account_id
# R2_ACCESS_KEY_ID=your_access_key
# R2_SECRET_ACCESS_KEY=your_secret_key

# Cloudflare D1 Configuration (para desenvolvimento local)
# Em produção, estas são gerenciadas pelo Wrangler
# D1_DATABASE_ID=your_database_id_here